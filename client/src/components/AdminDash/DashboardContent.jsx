import React from "react";
import { Typography, Card, CardContent } from "@mui/material";

const DashboardContent = () => (
  <Card>
    <CardContent>
      <Typography variant="h5">Dashboard Content component</Typography>
    </CardContent>
  </Card>
);

export default DashboardContent;
